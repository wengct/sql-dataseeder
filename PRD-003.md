## 🎯 Feature Request: 整合 Faker.js 提供更真實的假資料生成

### 問題描述

目前 `FakeDataService` 使用自行實作的隨機字串產生器，只能產生像 `'a8Kj2mNpQx'` 這樣無意義的英數字串。這在實際測試場景中有以下問題：

1. **資料不夠真實** - 產生的資料無法反映真實業務場景
2. **欄位語意無法辨識** - 無法根據欄位名稱（如 `Email`、`FirstName`、`Address`）產生對應的合理資料
3. **偵錯困難** - 隨機字串不易識別，增加除錯難度

### 建議解決方案

整合 [@faker-js/faker](https://github.com/faker-js/faker) 來產生更有意義的假資料。

### 功能需求

#### 1. 智慧欄位名稱識別

根據欄位名稱自動選擇適當的 Faker 方法：

| 欄位名稱模式 | Faker 方法 |
|-------------|-----------|
| `*name*`, `*firstname*` | `faker.person.firstName()` |
| `*lastname*` | `faker.person.lastName()` |
| `*email*` | `faker.internet.email()` |
| `*phone*`, `*mobile*` | `faker.phone.number()` |
| `*address*` | `faker.location.streetAddress()` |
| `*city*` | `faker.location.city()` |
| `*country*` | `faker.location.country()` |
| `*company*` | `faker.company.name()` |
| `*url*`, `*website*` | `faker.internet.url()` |
| `*description*`, `*notes*` | `faker.lorem.sentences()` |
| `*username*` | `faker.internet.userName()` |
| `*password*` | `faker.internet.password()` |
| `*date*`, `*created*`, `*updated*` | `faker.date.past()` |
| `*price*`, `*amount*`, `*cost*` | `faker.commerce.price()` |
| 其他 | 退回原本的隨機字串產生 |

#### 2. 多語系支援

- 預設使用 `en` (英文)
- 可設定為 `zh_TW` (繁體中文) 或其他語系
- 透過 VS Code 設定 (`settings.json`) 配置

```json
{
  "sql-dataseeder.fakerLocale": "zh_TW"
}
```

#### 3. 向下相容性

- 當 Faker 無法識別欄位時，退回使用現有的隨機英數字串產生器
- 確保所有現有的資料類型支援仍正常運作

### 技術實作建議

#### 新增依賴

```json
{
  "dependencies": {
    "@faker-js/faker": "^9.0.0"
  }
}
```

#### 程式結構變更

1. **新增欄位名稱解析器** - `src/services/columnNameParser.ts`
   - 解析欄位名稱，回傳適當的 Faker 方法類型

2. **修改 FakeDataService** - fakeDataService.ts
   - 在產生字串類型值時，先嘗試使用 Faker
   - 無法匹配時退回原本邏輯

3. **新增設定項** - package.json contributes.configuration
   - `sql-dataseeder.fakerLocale`: 語系設定
   - `sql-dataseeder.useFaker`: 是否啟用 Faker (預設 true)

### 驗收標準

- [ ] 安裝 `@faker-js/faker` 依賴
- [ ] 實作欄位名稱解析邏輯
- [ ] 整合 Faker 至 `FakeDataService`
- [ ] 新增 VS Code 設定項
- [ ] 撰寫單元測試
- [ ] 更新 README 文件

### 範例

**Before (現在)**

```sql
INSERT INTO [Users] ([FirstName], [Email], [Phone])
VALUES ('a8Kj2mNpQx', 'B2xYzWvU3q', 'N7pLm4Ks9R');
```

**After (整合後)**

```sql
INSERT INTO [Users] ([FirstName], [Email], [Phone])
VALUES ('John', 'john.doe@example.com', '555-123-4567');
```

### 相關資源

- [Faker.js 官方文件](https://fakerjs.dev/)
- [Faker.js GitHub](https://github.com/faker-js/faker)
